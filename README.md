# Hello World

This project implements **Hello World** in C and Assembly without the standard library or any other external dependencies, written for ARM64 and linux. It contains both ARM64 assembly and manual system calls.